<div class="px-6 py-4 border-b border-gray-100 bg-gray-50 flex flex-col md:flex-row md:items-center md:justify-between gap-3">
         <div>
           <h3 class="text-lg font-bold text-gray-900">{{ course.offering.course }}</h3>
-          <p class="text-sm text-gray-600">{{ course.offering.class_group|default:"" }} · {{ course.offering.term|default:"" }}</p>
+          <p class="text-sm text-gray-600">{{ course.offering.class_group|default:"" }} · {{ course.offering.term|default:"" }}{% if course.scheme %} · {{ course.scheme.name }}{% endif %}</p>
         </div>
         <div class="flex flex-wrap gap-2 text-sm">
           <span class="inline-flex px-3 py-1 rounded-full bg-primary-100 text-primary-800 font-bold">
-            {% if course.weighted_percentage != None %}{{ course.weighted_percentage }}%{% elif course.simple_percentage != None %}{{ course.simple_percentage }}%{% else %}-{% endif %}
+            {% if course.scheme and not course.is_complete %}Incomplete{% elif course.weighted_percentage != None %}{{ course.weighted_percentage }}%{% elif course.simple_percentage != None %}{{ course.simple_percentage }}%{% else %}-{% endif %}
           </span>
           <span class="inline-flex px-3 py-1 rounded-full bg-gray-100 text-gray-800 font-bold">Grade {{ course.grade }}</span>
           <span class="inline-flex px-3 py-1 rounded-full bg-blue-100 text-blue-800 font-bold">{{ course.remark }}</span>
         </div>
       </div>
 
+      {% if course.component_results %}
+      <div class="border-b border-gray-100 bg-blue-50/50 px-6 py-4">
+        <p class="text-xs font-black uppercase tracking-wider text-blue-800">Weighted components</p>
+        <div class="mt-3 grid gap-3 md:grid-cols-2 xl:grid-cols-3">
+          {% for component in course.component_resu